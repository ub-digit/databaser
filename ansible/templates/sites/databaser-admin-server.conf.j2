<VirtualHost *:80>
    ServerName {{ databaser_admin_server_hostname }}
    Redirect permanent / https://{{ databaser_admin_server_hostname }}/
</VirtualHost>

<VirtualHost *:443>
    ServerName {{ databaser_admin_server_hostname }}
    FallbackResource /index.html

    ProxyPreserveHost Off
    ProxyPass        / http://localhost:{{ databaser_admin_server_host_port }}/
    ProxyPassReverse / http://localhost:{{ databaser_admin_server_host_port }}/

    SSLEngine on
    SSLCertificateFile /etc/ssl/apache2/certs/ub-gu-se.pem
    SSLCertificateKeyFile /etc/ssl/apache2/private/ub-gu-se.key
    SSLCertificateChainFile /etc/ssl/apache2/certs/ub-gu-se-chain.pem
</VirtualHost>
